
/*
#ifndef setPosition_h
#define setPosition_h
#include "setValue.h"

class SetPosition : public SetValue
{
    void updateDisplay(long topValue);
};

void SetPosition::updateDisplay(long topValue_p)
{
    textTop =T_MANUAL_POSITION + formatValue(topValue_p);
    byte len = T_MANUAL_POSITION.length()
    while(textTop.length < 0x10)
    {
        textTop = 
    }

}
#endif

*/